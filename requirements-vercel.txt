# Vercel Python Functions依赖
# 优化版本，减少包大小和冷启动时间

# 核心依赖
requests>=2.31.0,<3.0.0
pymongo>=4.6.0,<5.0.0
beautifulsoup4>=4.12.0,<5.0.0
lxml>=4.9.0,<5.0.0

# 日期时间处理
python-dateutil>=2.8.0,<3.0.0

# 环境变量管理
python-dotenv>=1.0.0,<2.0.0

# JSON处理（Python内置，但确保兼容性）
# json - 内置模块

# 轻量级HTTP客户端（替代requests，更快）
httpx>=0.25.0,<1.0.0

# 数据验证
pydantic>=2.5.0,<3.0.0

# 时区处理
pytz>=2023.3

# 错误处理和重试
tenacity>=8.2.0,<9.0.0

# 日志处理
structlog>=23.2.0,<24.0.0

# 缓存（可选）
cachetools>=5.3.0,<6.0.0

# 数据序列化
orjson>=3.9.0,<4.0.0

# 异步支持（可选）
aiohttp>=3.9.0,<4.0.0
aiofiles>=23.2.0,<24.0.0

# 监控和指标（轻量级）
psutil>=5.9.0,<6.0.0

# 安全相关
cryptography>=41.0.0,<42.0.0

# 数据处理
pandas>=2.1.0,<3.0.0
numpy>=1.24.0,<2.0.0

# Web框架支持（如果需要）
fastapi>=0.104.0,<1.0.0
uvicorn>=0.24.0,<1.0.0

# 数据库连接池
motor>=3.3.0,<4.0.0

# 配置管理
pydantic-settings>=2.1.0,<3.0.0

# 类型检查
typing-extensions>=4.8.0,<5.0.0

# 测试工具（开发时使用）
pytest>=7.4.0,<8.0.0
pytest-asyncio>=0.21.0,<1.0.0

# 代码质量
black>=23.9.0,<24.0.0
flake8>=6.1.0,<7.0.0
mypy>=1.6.0,<2.0.0